<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/circle_shape_radius" /> <!-- Ajusta el radio según sea necesario -->
    <solid android:color="@color/main_color" />
    <stroke
        android:width="3dp"
        android:color="@color/main_color" /> <!-- Ajusta el ancho y el color del borde según sea necesario -->
</shape>
